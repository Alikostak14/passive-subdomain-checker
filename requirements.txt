# Temel Kullanım (Varsayılan 15 thread ve otomatik dosya ismi)
./subdomain_finder.sh example.com

# Özelleştirilmiş Kullanım (30 thread paralel çalışma ve özel çıktı dosyası)
./subdomain_finder.sh example.com -o hedef_sonuc.txt -t 30
